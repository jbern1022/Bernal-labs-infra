steps:
  - name: validate
    image: alpine:latest
    commands:
      - echo "Pipeline running"
      - echo "Repo is $CI_REPO_NAME"
      - find . -name "docker-compose.yml" -exec echo "Found: {}" \;
